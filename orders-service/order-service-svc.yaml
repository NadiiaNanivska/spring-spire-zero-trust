apiVersion: v1
kind: Service
metadata:
  name: orders-service
  namespace: spire
spec:
  selector:
    app: orders-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
